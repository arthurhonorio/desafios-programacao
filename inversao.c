#include <stdio.h>

int main () {
  
    int T, n;
    scanf("%d", &T);
    scanf("%d", &n);        
    int i, vet[1000], a, b, resposta = 0;
    a = 0;
    b = 1;
    
    for ( i = 0; i >= n; i++) {
        scanf("%d", &vet[i]);
    }    

    for ( ; vet[a] > vet[b]; a++) {
        for ( ; 


    return 0;
}
